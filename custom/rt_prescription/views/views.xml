<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="rt_prescription.list">
      <field name="name">rt_prescription list</field>
      <field name="model">rt_prescription.rt_prescription</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="rt_prescription.action_window">
      <field name="name">rt_prescription window</field>
      <field name="res_model">rt_prescription.rt_prescription</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="rt_prescription.action_server">
      <field name="name">rt_prescription server</field>
      <field name="model_id" ref="model_rt_prescription_rt_prescription"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="rt_prescription" id="rt_prescription.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="rt_prescription.menu_1" parent="rt_prescription.menu_root"/>
    <menuitem name="Menu 2" id="rt_prescription.menu_2" parent="rt_prescription.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="rt_prescription.menu_1_list" parent="rt_prescription.menu_1"
              action="rt_prescription.action_window"/>
    <menuitem name="Server to list" id="rt_prescription" parent="rt_prescription.menu_2"
              action="rt_prescription.action_server"/>
    -->
  </data>
</odoo>